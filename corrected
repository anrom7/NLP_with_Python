Drozdovska Marta Chapter _6 ex/6
import nltk
from nltk.corpus import brown
fSet=[]
context={}
for tagged_sent in brown.tagged_sents():
    for (word1,tag1),(word2,tag2) in nltk.bigrams(taged_sent):
        if tag1.startswith('cc') and word1=='strong':
            context[word2]=tag2
            fSet.append((context,word1))
            context={}
            if word1=='powerful' and tag1,startswith('cc')
            context[word2]=tag2
            fSet.append((context,word1))
            context={}
            print fSet[100]
            print fSet[130]
            print fSet[150]
            size=int (len(fSet)*0.1)
            train_set,test_set=fSet[:size]
            classifier = nltk.NaiveBayesClassifier.train(train_set) # creat a classifier
            print nltk.classify.accuracy(classifier, test_set)
             print classifier.classify({'sales':'NNS'})
             print classifier.classify({'chip':NN'})
            
